ANTHROPIC_API_KEY=

# Studio Coach is the orchestrator — it spawns other profiles as subagents.
# Each child profile uses its own .env, so this profile only needs the model
# provider key plus any shared infra keys you want available.

# Optional: shared messaging / status integrations
# SLACK_BOT_TOKEN=
# LINEAR_API_KEY=
# NOTION_API_KEY=
